<%= render 'shared/page_title', title: "Crear un nuevo ingrediente" %>

<%= render 'shared/errors', obj: @ingredient %>

<div class="row">
	<div class="well col-md-8 col-md-offset-2">
		<%= form_for @ingredient do |f| %>
			<%= f.label :name %> 
			<%= f.text_field :name %>

			<%= f.submit 'Crear ingrediente', class: "btn btn-success" %> 

		 <%end%>
	</div>
</div>